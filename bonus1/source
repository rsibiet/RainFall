#include <stdlib.h>
#include <unistd.h>
#include <string.h>

int		main(int ac, char **av)
{
	int		nb;
	int 	target;
	char 	dst[36];

	if ((nb = atoi(av[1])) > 9)
		exit(1);
	nb *= 4;
	memcpy(dst, av[2], (size_t)nb);
	if (target == 1464814662)
		execl("/bin/sh", "sh");
	return (0);
}
